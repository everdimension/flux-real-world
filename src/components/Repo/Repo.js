import React, { PropTypes } from 'react';

const propTypes = {};

class Repo extends React.Component {
  constructor() {
    super();
  }

  render() {
    return (
      <div>repo</div>
    );
  }
}

Repo.propTypes = propTypes;

export default Repo;
